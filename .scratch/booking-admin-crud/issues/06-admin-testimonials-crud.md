# 06: Admin CRUD Testimoni

**What to build:** Menu Testimoni baru di admin (list + buat + edit + hapus + toggle aktif). Form → `/api/admin/testimonials`. Tambah item menu di `AdminLayoutClient`.

**Blocked by:** None (can start immediately)

**Status:** ready-for-agent

- [ ] API `/api/admin/testimonials` (POST/PUT/DELETE)
- [ ] Page list + form (`/admin/testimonials`, `/new`, `/[id]`)
- [ ] Tambah menu "Testimoni" di sidebar
- [ ] Typecheck hijau
